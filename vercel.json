{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && pnpm install --frozen-lockfile && pnpm build",
  "functions": {
    "api.py": {
      "includeFiles": "{artifacts/**,assets/demo_samples/**,frontend/dist/**}",
      "maxDuration": 30
    }
  }
}
